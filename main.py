from classes.readweb import ReadWeb as rweb
from classes.ir import irsystem as irs

# max limit test = 10
limit = 10
# obj_rweb = rweb(limit)
# obj_rweb.run()  # run just for first time
#
obj_irs = irs(limit)
# obj_irs.normalizer_data()
# obj_irs.create_posting_list()
